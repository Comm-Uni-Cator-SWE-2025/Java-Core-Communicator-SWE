/**
 * Implement
 * > A simple priority queue which has a nextPacket function
 * which gives the next highest priority packet
 * > A addPacket function which adds a packet to the queue
 *
 */

public class PriorityQueue {
}
